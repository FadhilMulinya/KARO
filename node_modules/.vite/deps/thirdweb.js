import {
  eth_estimateGas
} from "./chunk-ND3TNMCQ.js";
import {
  eth_getTransactionCount
} from "./chunk-6XETUKRO.js";
import {
  isValidSignature
} from "./chunk-OFLOZYR7.js";
import {
  serializeTransaction,
  signTransaction
} from "./chunk-3G4ZWRL2.js";
import {
  eth_getLogs,
  getBuyWithCryptoHistory,
  getContractEvents,
  isBaseTransactionOptions,
  simulateTransaction,
  watchContractEvents
} from "./chunk-P2ZR67AF.js";
import {
  estimateGasCost,
  getBuyWithCryptoQuote,
  getBuyWithCryptoStatus,
  getBuyWithCryptoTransfer
} from "./chunk-5TUD5A7Y.js";
import {
  sendBatchTransaction
} from "./chunk-ES4JQ4WC.js";
import {
  sendAndConfirmTransaction
} from "./chunk-KV7UIEZG.js";
import {
  ADDRESS_ZERO,
  NATIVE_TOKEN_ADDRESS,
  ZERO_ADDRESS
} from "./chunk-F2NDM2JU.js";
import {
  eth_blockNumber,
  eth_getTransactionReceipt,
  waitForReceipt,
  watchBlockNumber
} from "./chunk-5UMPSIOZ.js";
import {
  parseEventLogs,
  prepareEvent
} from "./chunk-7AXORLT7.js";
import "./chunk-ZMNEMXKJ.js";
import "./chunk-DSLEEGKS.js";
import {
  prepareContractCall
} from "./chunk-TOLLXXKE.js";
import {
  concatHex
} from "./chunk-HIZG7GSI.js";
import "./chunk-WWY7S4YD.js";
import {
  eth_getBalance
} from "./chunk-OULA2JUL.js";
import {
  sendTransaction
} from "./chunk-YXDWDMRE.js";
import "./chunk-HH2NFLAA.js";
import {
  estimateGas,
  eth_gasPrice,
  eth_maxPriorityFeePerGas,
  getGasPrice,
  resolveContractAbi,
  toSerializableTransaction
} from "./chunk-QZO2K6UY.js";
import {
  eth_getBlockByNumber
} from "./chunk-2UJXDFOT.js";
import "./chunk-2FH4XCFL.js";
import "./chunk-MJIXXO2A.js";
import "./chunk-SHLB24UB.js";
import {
  fromGwei,
  toEther,
  toTokens,
  toUnits,
  toWei
} from "./chunk-HAADYJEF.js";
import {
  prepareTransaction
} from "./chunk-WAYCTEWF.js";
import "./chunk-6S45W5AQ.js";
import {
  eth_call,
  readContract
} from "./chunk-SUYUKCS3.js";
import "./chunk-6ZKOZ5T4.js";
import {
  encode
} from "./chunk-NVBBE2QQ.js";
import {
  serializeSignature
} from "./chunk-FL6IJKMT.js";
import {
  eth_sendRawTransaction
} from "./chunk-CMXLKATA.js";
import {
  getContract
} from "./chunk-V4R6W4TB.js";
import {
  eth_getCode
} from "./chunk-XIZJSHMT.js";
import {
  getRpcClient
} from "./chunk-BFYMVFVP.js";
import "./chunk-7IZG4EP5.js";
import {
  encodeAbiParameters
} from "./chunk-YQ3CEJLX.js";
import {
  formatBlock,
  formatTransaction,
  hashTypedData
} from "./chunk-DZFAH7EW.js";
import {
  getAddress,
  isAddress
} from "./chunk-CLWLXFWW.js";
import {
  boolToBytes,
  hexToBytes,
  keccak256,
  numberToBytes,
  stringToBytes,
  toBytes
} from "./chunk-JKNO5SWR.js";
import {
  assertSize,
  boolToHex,
  fromHex,
  hexToBigInt,
  hexToBool,
  hexToNumber,
  hexToString,
  hexToUint8Array,
  isHex,
  numberToHex,
  padHex,
  stringToHex,
  toHex,
  uint8ArrayToHex
} from "./chunk-BNK74YTA.js";
import "./chunk-GDDTQ4UI.js";
import {
  equalBytes
} from "./chunk-CJ2CQ55J.js";
import "./chunk-2FWTASOS.js";
import {
  sha256
} from "./chunk-3IMKMHHP.js";
import {
  encodeDeployData,
  sliceHex,
  universalSignatureValidatorAbi,
  universalSignatureValidatorByteCode
} from "./chunk-XHFXQBSA.js";
import "./chunk-JOR3C3P6.js";
import "./chunk-RBM7JEQD.js";
import "./chunk-RDXQI2QQ.js";
import {
  defineChain
} from "./chunk-TESS5WGS.js";
import "./chunk-6SJGCNT7.js";
import "./chunk-3R4VSFDT.js";
import "./chunk-EZ4JTQAA.js";
import {
  LruMap
} from "./chunk-SHWHM7JY.js";
import "./chunk-MSFXBLHD.js";

// node_modules/thirdweb/dist/esm/utils/hashing/sha256.js
function sha2562(value, to) {
  const bytes = sha256(isHex(value, { strict: false }) ? hexToUint8Array(value) : value);
  if (to === "bytes") {
    return bytes;
  }
  return uint8ArrayToHex(bytes);
}

// node_modules/thirdweb/dist/esm/utils/client-id.js
var cache = new LruMap(4096);
function computeClientIdFromSecretKey(secretKey) {
  if (cache.has(secretKey)) {
    return cache.get(secretKey);
  }
  const cId = sha2562(stringToBytes(secretKey)).slice(2, 34);
  cache.set(secretKey, cId);
  return cId;
}

// node_modules/thirdweb/dist/esm/client/client.js
function createThirdwebClient(options) {
  const { clientId, secretKey, ...rest } = options;
  if (secretKey) {
    return {
      ...rest,
      clientId: computeClientIdFromSecretKey(secretKey),
      secretKey
    };
  }
  if (clientId) {
    return {
      ...rest,
      clientId: options.clientId,
      secretKey: void 0
    };
  }
  throw new Error("clientId or secretKey must be provided");
}

// node_modules/thirdweb/dist/esm/rpc/actions/eth_getBlockByHash.js
async function eth_getBlockByHash(request, params) {
  const includeTransactions = params.includeTransactions ?? false;
  const block = await request({
    method: "eth_getBlockByHash",
    params: [params.blockHash, includeTransactions]
  });
  if (!block) {
    throw new Error("Block not found");
  }
  return formatBlock(block);
}

// node_modules/thirdweb/dist/esm/rpc/actions/eth_getStorageAt.js
async function eth_getStorageAt(request, params) {
  return await request({
    method: "eth_getStorageAt",
    params: [params.address, params.position, params.blockTag ?? "latest"]
  });
}

// node_modules/thirdweb/dist/esm/rpc/actions/eth_getTransactionByHash.js
async function eth_getTransactionByHash(request, params) {
  const receipt = await request({
    method: "eth_getTransactionByHash",
    params: [params.hash]
  });
  if (!receipt) {
    throw new Error("Transaction not found.");
  }
  return formatTransaction(receipt);
}

// node_modules/thirdweb/dist/esm/transaction/resolve-method.js
function resolveMethod(method) {
  return async (contract) => {
    const resolvedAbi = await resolveContractAbi(contract);
    const abiFunction = resolvedAbi.find((item) => {
      if (item.type !== "function") {
        return false;
      }
      return item.name === method;
    });
    if (!abiFunction) {
      throw new Error(`could not find function with name "${method}" in abi`);
    }
    return abiFunction;
  };
}

// node_modules/thirdweb/dist/esm/utils/encoding/helpers/trim.js
function trim(hexOrBytes, { dir = "left" } = {}) {
  let data = typeof hexOrBytes === "string" ? hexOrBytes.replace("0x", "") : hexOrBytes;
  let sliceLength = 0;
  for (let i = 0; i < data.length - 1; i++) {
    if (data[dir === "left" ? i : data.length - i - 1].toString() === "0") {
      sliceLength++;
    } else {
      break;
    }
  }
  data = dir === "left" ? data.slice(sliceLength) : data.slice(0, data.length - sliceLength);
  if (typeof hexOrBytes === "string") {
    if (data.length === 1 && dir === "right") {
      data = `${data}0`;
    }
    return `0x${data.length % 2 === 1 ? `0${data}` : data}`;
  }
  return data;
}

// node_modules/thirdweb/dist/esm/utils/encoding/from-bytes.js
function fromBytes(bytes, toOrOpts) {
  const opts = typeof toOrOpts === "string" ? { to: toOrOpts } : toOrOpts;
  switch (opts.to) {
    case "number":
      return bytesToNumber(bytes, opts);
    case "bigint":
      return bytesToBigInt(bytes, opts);
    case "boolean":
      return bytesToBool(bytes, opts);
    case "string":
      return bytesToString(bytes, opts);
    default:
      return uint8ArrayToHex(bytes, opts);
  }
}
function bytesToBigInt(bytes, opts = {}) {
  if (typeof opts.size !== "undefined") {
    assertSize(bytes, { size: opts.size });
  }
  const hex = uint8ArrayToHex(bytes, opts);
  return hexToBigInt(hex, opts);
}
function bytesToBool(bytes_, opts = {}) {
  let bytes = bytes_;
  if (typeof opts.size !== "undefined") {
    assertSize(bytes, { size: opts.size });
    bytes = trim(bytes);
  }
  if (bytes.length > 1 || bytes[0] && bytes[0] > 1) {
    throw new Error(`Invalid boolean representation: ${bytes}`);
  }
  return Boolean(bytes[0]);
}
function bytesToNumber(bytes, opts = {}) {
  if (typeof opts.size !== "undefined") {
    assertSize(bytes, { size: opts.size });
  }
  const hex = uint8ArrayToHex(bytes, opts);
  return hexToNumber(hex, opts);
}
function bytesToString(bytes_, opts = {}) {
  let bytes = bytes_;
  if (typeof opts.size !== "undefined") {
    assertSize(bytes, { size: opts.size });
    bytes = trim(bytes, { dir: "right" });
  }
  return new TextDecoder().decode(bytes);
}

// node_modules/thirdweb/dist/esm/auth/constants.js
var ERC_6492_MAGIC_VALUE = "0x6492649264926492649264926492649264926492649264926492649264926492";

// node_modules/thirdweb/dist/esm/auth/is-erc6492-signature.js
function isErc6492Signature(signature) {
  return sliceHex(signature, -32) === ERC_6492_MAGIC_VALUE;
}

// node_modules/thirdweb/dist/esm/auth/serialize-erc6492-signature.js
function serializeErc6492Signature({ address, data, signature }) {
  return concatHex([
    encodeAbiParameters([{ type: "address" }, { type: "bytes" }, { type: "bytes" }], [address, data, signature]),
    ERC_6492_MAGIC_VALUE
  ]);
}

// node_modules/thirdweb/dist/esm/auth/verify-hash.js
async function verifyHash({ hash, signature, address, client, chain, accountFactory }) {
  const signatureHex = (() => {
    if (isHex(signature))
      return signature;
    if (typeof signature === "object" && "r" in signature && "s" in signature)
      return serializeSignature(signature);
    if (signature instanceof Uint8Array)
      return fromBytes(signature, "hex");
    throw new Error(`Invalid signature type for signature ${signature}: ${typeof signature}`);
  })();
  const wrappedSignature = await (async () => {
    if (!accountFactory)
      return signatureHex;
    if (isErc6492Signature(signatureHex))
      return signatureHex;
    return serializeErc6492Signature({
      address: accountFactory.address,
      data: accountFactory.verificationCalldata,
      signature: signatureHex
    });
  })();
  const verificationData = encodeDeployData({
    abi: universalSignatureValidatorAbi,
    args: [address, hash, wrappedSignature],
    bytecode: universalSignatureValidatorByteCode
  });
  const rpcRequest = getRpcClient({
    chain,
    client
  });
  try {
    const result = await eth_call(rpcRequest, {
      data: verificationData
    });
    const hexResult = isHex(result) ? toBytes(result) : result;
    return equalBytes(hexResult, toBytes("0x1"));
  } catch {
    const validEip1271 = await verifyEip1271Signature({
      hash,
      signature: signatureHex,
      contract: getContract({
        chain,
        address,
        client
      })
    }).catch(() => false);
    if (validEip1271) {
      return true;
    }
    return false;
  }
}
var EIP_1271_MAGIC_VALUE = "0x1626ba7e";
async function verifyEip1271Signature({ hash, signature, contract }) {
  const result = await isValidSignature({
    hash,
    signature,
    contract
  });
  return result === EIP_1271_MAGIC_VALUE;
}

// node_modules/thirdweb/dist/esm/auth/verify-typed-data.js
async function verifyTypedData({ address, signature, client, chain, accountFactory, message, domain, primaryType, types }) {
  const messageHash = hashTypedData({
    message,
    domain,
    primaryType,
    types
  });
  return verifyHash({
    hash: messageHash,
    signature,
    address,
    chain,
    client,
    accountFactory
  });
}
export {
  ADDRESS_ZERO,
  NATIVE_TOKEN_ADDRESS,
  ZERO_ADDRESS,
  boolToBytes,
  boolToHex,
  bytesToBigInt,
  bytesToBool,
  bytesToNumber,
  bytesToString,
  concatHex,
  createThirdwebClient,
  defineChain,
  encode,
  estimateGas,
  estimateGasCost,
  eth_blockNumber,
  eth_call,
  eth_estimateGas,
  eth_gasPrice,
  eth_getBalance,
  eth_getBlockByHash,
  eth_getBlockByNumber,
  eth_getCode,
  eth_getLogs,
  eth_getStorageAt,
  eth_getTransactionByHash,
  eth_getTransactionCount,
  eth_getTransactionReceipt,
  eth_maxPriorityFeePerGas,
  eth_sendRawTransaction,
  fromBytes,
  fromGwei,
  fromHex,
  getAddress,
  getBuyWithCryptoHistory,
  getBuyWithCryptoQuote,
  getBuyWithCryptoStatus,
  getBuyWithCryptoTransfer,
  getContract,
  getContractEvents,
  getGasPrice,
  getRpcClient,
  hexToBigInt,
  hexToBool,
  hexToBytes,
  hexToNumber,
  hexToString,
  hexToUint8Array,
  isAddress,
  isBaseTransactionOptions,
  isHex,
  keccak256,
  numberToBytes,
  numberToHex,
  padHex,
  parseEventLogs,
  prepareContractCall,
  prepareEvent,
  prepareTransaction,
  readContract,
  resolveMethod,
  sendAndConfirmTransaction,
  sendBatchTransaction,
  sendTransaction,
  serializeTransaction,
  sha2562 as sha256,
  signTransaction,
  simulateTransaction,
  stringToBytes,
  stringToHex,
  toBytes,
  toEther,
  toHex,
  toSerializableTransaction,
  toTokens,
  toUnits,
  toWei,
  uint8ArrayToHex,
  verifyTypedData,
  waitForReceipt,
  watchBlockNumber,
  watchContractEvents
};
//# sourceMappingURL=thirdweb.js.map
